#include <stdio.h>
int main(){
    int n;
    int array[n];
    for(int i=0;i<n;i++){
        scanf("%d",&array[i]);
    }
    int max=arr[0];
    for(int i=0;i<n;i++){
        if(arr[i]>arr[0]){
            max=arr[i];
        }
    }
    return max;
}